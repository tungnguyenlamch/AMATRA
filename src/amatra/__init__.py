"""AMATRA integration boundary package.

Thin, stable interface layer between the research/model code in
``src/pipeline`` and consumers such as the Qt app package ``amatra_app``
(under ``app/``). Consumers should only import from ``amatra.*``.
"""
